<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.redcross.mapper.UserMapper">
    <!-- 查询所有用户 -->
    <select id="getAllUsers" resultType="com.example.redcross.entity.User">
        SELECT user_id, user_name, picture_url, email, account, password, user_type
        FROM user
    </select>
    <!-- 注册用户 -->
    <insert id="register" parameterType="com.example.redcross.entity.User">
        INSERT INTO user (user_name, picture_url, email, account, password, user_type)
        VALUES (#{userName}, #{pictureUrl}, #{email}, #{account}, #{password}, #{userType})
    </insert>
    <!-- 登录 -->
    <select id="login" parameterType="com.example.redcross.entity.User" resultType="com.example.redcross.entity.User">
        SELECT * FROM user WHERE account = #{account} AND password = #{password}
    </select>
</mapper>